-- Логотип агентства: одно поле-ключ, не отдельная модель — логотип один,
-- версии не хранятся, не модерируется (в отличие от фото анкет).

-- AlterTable
ALTER TABLE "Company" ADD COLUMN     "logoStorageKey" TEXT;
