-- Свободные теги анкеты из прототипа. Заменены справочником услуг: набранные
-- руками, они расходились в написании («ужин» / «Ужин» / «dinner»), не
-- переводились на три языка и не годились для фильтров.
--
-- Поле пустое у всех анкет — проверено перед удалением. GIN-индекс по нему
-- удаляется вместе с колонкой, но снимаем его явно: так порядок виден в
-- миграции, а не выводится из поведения Postgres.
DROP INDEX IF EXISTS "Profile_tags_idx";
ALTER TABLE "Profile" DROP COLUMN "tags";
