/**
 * Заглушки на время вёрстки биллинга.
 *
 * Баланс и подписка приходят с сервера, которого ещё нет, — но экраны
 * кабинета должны показывать их уже сейчас. Всё выдуманное собрано в одном
 * файле: при подключении API он удаляется целиком, и TypeScript сам покажет
 * каждое место, которое надо перевести на реальные данные.
 *
 * Ничего отсюда не должно попасть на публичные страницы.
 */

/** Текущий баланс в GlowCoin. */
export const MOCK_BALANCE_GC = 420;

/** Дата фиксированная, а не «сегодня + N»: так на скриншотах и в тестах
 *  один и тот же экран, и мок не выглядит правдоподобнее, чем он есть. */
export const MOCK_SUBSCRIPTION = {
  term: 'm6',
  expiresAt: '2027-01-15T00:00:00.000Z',
  autoRenew: true,
} as const;
